const AudioPlayer = {
  baseStyle: {
    invalid: {
      backgroundColor: "ui.bg.default",
      padding: "s",
    },
    helperErrorText: {
      fontSize: "text.caption",
    },
  },
};

export default AudioPlayer;
