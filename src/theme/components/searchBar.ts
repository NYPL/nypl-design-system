const SearchBar = {
  baseStyle: {
    display: "flex",
    marginBottom: {
      base: "xs",
      md: "auto",
    },
    flexFlow: {
      base: "column nowrap",
      md: "row nowrap",
    },
  },
};

export default SearchBar;
