const SearchBar = {
  parts: ["select"],
  baseStyle: {
    display: "flex",
    marginBottom: {
      base: "xs",
      md: "auto",
    },
    flexFlow: {
      base: "column nowrap",
      md: "row nowrap",
    },
    select: {
      marginBottom: "0",
    },
  },
};

export default SearchBar;
