/**
 * Note: these styles are only used when the
 * DatePicker is in "date range" mode.
 */
const DatePicker = {
  parts: ["subLabels"],
  baseStyle: {
    subLabels: {
      label: {
        fontSize: "12px",
        marginBottom: "xs",
      },
    },
  },
};

export default DatePicker;
