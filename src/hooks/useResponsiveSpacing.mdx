import { Meta, Source } from "@storybook/addon-docs/blocks";
import { Box } from "@chakra-ui/react";

import Banner from "../components/Banner/Banner";
import ComponentDocsHeader from "../utils/components/ComponentDocsHeader";
import useResponsiveSpacing from "../hooks/useResponsiveSpacing";

<Meta title="Hooks/useResponsiveSpacing" />

<ComponentDocsHeader
  category="Reservoir Custom Hook"
  componentName="useResponsiveSpacing"
  summary="Provides responsive spacing values that align with NYPL design standards"
  versionAdded="4.0.0"
  versionLatest="4.0.0"
/>

<Source
  code={`
  import {
    useResponsiveSpacing,
  } from "@nypl/design-system-react-components";
  // ...
  const {
    responsiveGap,
    responsiveMargin,
    responsivePadding,
  } = useResponsiveSpacing();
`}
  language="tsx"
/>

## Usage

The `useResponsiveSpacing` hook returns responsive gap and padding values that
align with NYPL design standards for responsive grids and columns. The values
will adjust based on the width of the viewport.

<Banner
  content={
    <>
      <strong>RECOMMENDATION:</strong> DS components, including the Template
      component, use this hook and the preset values internally, so this hook
      should be used for custom layouts or components.
    </>
  }
  mb="s"
  variant="recommendation"
/>

## responsiveGap

The `responsiveGap` object is intended to be used as the gap values for
repeating grids and table.

- Mobile: `1rem` (16px)
- Tablet: `1.5rem` (24px)
- Desktop: `2rem` (32px)

<Source
  code={`
  const {
    responsiveGap
  } = useResponsiveSpacing();
  // ...
  <Box 
    display="grid"
    gap={responsiveGap}
    gridTemplateColumns="repeat(3, 1fr)"
  >
    <div>...</div>
    <div>...</div>
    <div>...</div>
    <div>...</div>
    <div>...</div>
    <div>...</div>
  </Box>
`}
  language="tsx"
/>

## responsiveMargin and responsivePadding

The `responsiveMargin` and `responsivePadding` objects are intended to be used
as the values for the left and right spacing on any element that could
potentially touch the edge of the browser window. The values for each object are
the same, but separate objects have been provided to enable the use of the
object that aligns semantically with spacing methods used in consuming apps.

- Mobile: `1rem` (16px)
- Tablet: `1.5rem` (24px)
- Desktop: `1rem` (16px)

<Source
  code={`
  const {
    responsivePadding,
  } = useResponsiveSpacing();
  // ...
  <Box 
    maxWidth="1280px"
    px={responsivePadding}
  >
    <div>...</div>
  </Box>
`}
  language="tsx"
/>

## Visual example

- The example below illustrates the responsive padding and gap values in action.
- The `padding` is orange and the `gap` is blue.

Change the size of your browser window and watch the spacing values adjust as
the width of the window decreases and increases.

export const { responsiveGap, responsivePadding } = useResponsiveSpacing();
export const contentStyles = {
  bgColor: "ui.bg.default",
  color: "ui.typography.heading",
  p: "l",
  textAlign: "center",
};

<Box bgColor="ui.warning.primary" maxWidth="1280px" px={responsivePadding}>
  <Box
    bgColor="ui.link.primary"
    display="grid"
    gap={responsiveGap}
    gridTemplateColumns={{
      base: "1fr",
      sm: "repeat(2, 1fr)",
      md: "repeat(3, 1fr)",
      lg: "repeat(4, 1fr)",
    }}
  >
    <Box __css={contentStyles}>Content</Box>
    <Box __css={contentStyles}>Content</Box>
    <Box __css={contentStyles}>Content</Box>
    <Box __css={contentStyles}>Content</Box>
    <Box __css={contentStyles}>Content</Box>
    <Box __css={contentStyles}>Content</Box>
    <Box __css={contentStyles}>Content</Box>
    <Box __css={contentStyles}>Content</Box>
    <Box __css={contentStyles}>Content</Box>
    <Box __css={contentStyles}>Content</Box>
    <Box __css={contentStyles}>Content</Box>
    <Box __css={contentStyles}>Content</Box>
  </Box>
</Box>
