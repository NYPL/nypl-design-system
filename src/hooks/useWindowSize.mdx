import { Meta, Source } from "@storybook/addon-docs/blocks";

import ComponentDocsHeader from "../utils/components/ComponentDocsHeader";

<Meta title="Hooks/useWindowSize" />

<ComponentDocsHeader
  category="Reservoir Custom Hook"
  componentName="useWindowSize"
  summary="Provides dimensions of the current browser window"
  versionAdded="0.25.2"
  versionLatest="0.25.2"
/>

<Source
  code={`
  import {
    useWindowSize
  } from "@nypl/design-system-react-components";
  // ...
  const { height, width } = useWindowSize();
`}
  language="tsx"
/>

## Usage

This hook is based on [Typescript hooks](https://usehooks-typescript.com/react-hook/use-window-size).
It returns an object with `width` and `height` properties that can be used to
update a component's UI based on the size of the browser window.

For an example implementation, view the [Tabs component](.?path=/docs/components-overlays-switchers-tabs--docs).
