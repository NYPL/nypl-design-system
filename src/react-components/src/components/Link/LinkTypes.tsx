export enum LinkTypes {
    Action = "action",
    Backwards = "backwards",
    Button = "button",
    Default = "default",
    Forwards = "forwards",
}

export enum LinkIconPositions {
    Left = "left",
    Right = "right",
}
