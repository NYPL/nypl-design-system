import { ArgTypes, Canvas, Description, Meta, Source } from "@storybook/blocks";

import * as SocialMediaLinksStories from "./SocialMediaLinks.stories";
import Link from "../Link/Link";

<Meta of={SocialMediaLinksStories} />

This is a proof of concept that passing social media platform names and urls will generate links and icons. Styling
is not included in this proof.

## No Type Props

<Canvas of={SocialMediaLinksStories.Primary} />

## Type Props as Strings

<Canvas of={SocialMediaLinksStories.WithTypes} />

## Structured Type Props with URL Override

<Canvas of={SocialMediaLinksStories.WithUrls} />

## Mix of Structured and String Props with URL Override

<Canvas of={SocialMediaLinksStories.WithMix} />
