import { Meta, Source } from "@storybook/addon-docs/blocks";

import { Banner } from "../Banner/Banner";
import Image from "../Image/Image";
import Link from "../Link/Link";
import ComponentDocsHeader from "../../utils/components/ComponentDocsHeader";
import progressiveEnhancementExample from "/static/progressive-enhancement.png";
import { exampleWrapperStyles } from "../../utils/utils.ts";

<Meta title="Accessibility Guide/Progressive Enhancement" />

<ComponentDocsHeader
  category="Accessibility Guide"
  componentName="Progressive Enhancement"
  summary="Guide to building basic web functionality and user experience first, then layering features incrementally"
/>

## Table of Contents

- [General Information](#general-information)
- [NYPL and DS Problem](#nypl-and-ds-problem)
- [HTML Button Issue](#html-button-issue)
- [Component Audit](#component-audit)
- [General Recommendations](#general-recommendations)
- [Resources](#resources)

## General Information

Progressive enhancement is a frontend development strategy and mindset that
emphasizes building a "basic", functional version of a website or component first,
then adding more advanced features and enhancements after. This approach ensures
that the core functionality is accessible to all users, regardless of their device,
browser capabilities, or connection quality (not just the few who intentionally
disable JavaScript).

A key benefit of progressive enhancement is that it prioritizes a fast and
reliable first render of the page. By ensuring the base HTML delivers meaningful
content and functionality upfront, we can improve usability and equity for users
in bandwidth-constrained environments.

On the development side, this inclusive design philosophy focuses on knowing and
implementing web fundamentals, such as HTML and CSS, before heavily relying on
JavaScript. Client-side JavaScript for core functionality places the burden on
the user's device and browser to download, parse, and execute JavaScript code.

## NYPL and DS Problem

When JavaScript is turned off in the browser, certain NYPL user interfaces and
features do not work. There are various reasons why this is, including how the
application is architected, how data is fetched, how data is rendered, and
whether the vendors we are using support it. Many issues, however, point back
to DS components.

## HTML Button Issue

HTML `<button>`s can only work without JavaScript in the context of a form with
an attribute of `type="submit"` or `type="reset"` (which clears input elements).

However, there are several DS components that use the `Button` component (and
therefore the `<button>` HTML element) outside of a form, to open, close, or
perform other user actions. These components cannot fully function without
JavaScript.

## Component Audit

As of v3.6.1, the DS contains 25 fully functional components, and 34 components
that need JavaScript to work.

The following (25) components are fully functional without JavaScript:

- `Breadcrumbs`
- `Card`
- `FeaturedContent`
- `FieldSet`
- `Footer`
- `Form`
- `FormRow`
- `FormField`
- `HelperErrorText`
- `Hero`
- `HorizontalRule`
- `Icon`
- `Image`
- `Label`
- `Link`
- `List`
- `Logo`
- `SimpleGrid`
- `SkipNavigation`
- `StatusBadge`
- `StructuredContent`
- `StyledList`
- `Table`
- `Template`
- `Text`

The following (16) use the HTML `<button>` element and so can't be fully functional
without JavaScript:

- `Accordion`
- `AlphabetFilter`
- `Banner`
- `Button`
- `ButtonGroup`
- `Header`
- `Menu`
- `Modal`
- `MultiSelect`
- `MultiSelectGroup`
- `NewsletterSignup`
- `Notification`
- `Pagination`
- `SubNav`
- `Tabs`
- `TagSet`

The following (15) components could work without JavaScript if updated:

- `Checkbox`
- `CheckboxGroup`
- `FeedbackBox`
- `FilterBarInline`
- `FilterBarPopup`
- `ProgressIndicator`
- `Radio`
- `RadioGroup`
- `SearchBar`
- `Select`
- `SkeletonLoader`
- `Slider`
- `TextInput`
- `Toggle`
- `Tooltip`

The following (2) can't be fixed due to browser limitations:

- `AudioPlayer`
- `VideoPlayer`

The following (1) can't be fixed because it depends on an external library:

- `DatePicker`

## General Recommendations

Based on the [Engineering General JavaScript standard](https://github.com/NYPL/engineering-general/blob/main/standards/javscript.md),
having no-JavaScript fallbacks is preferable, but not required. Our recommendation is
to update what we can (depending on time and resources).

We acknowledge that various new pages and features being built for NYPL have
JavaScript-heavy requirements. We recommend and encourage teams to add a noscript
fallback message. This would be an HTML/CSS snippet (not React) that instructs patrons
to enable JavaScript to use the site.

## noscript Fallback Example

We recommend placing the following `noscript` snippet at the top of the
`<body>` tag in your HTML for the global message.

<Source
  language="html"
  code={`
  <noscript>
    <aside className="noscript-banner">
      <h2>JavaScript is disabled.</h2>
      <div>Some features may not work as expected.</div>
    </aside>
    <style>
      .noscript-banner {
        background: #f9e08e;
        color: #2e2e2e;
        padding: 16px;
        text-align: center;
        font-size: 16px;
      }
    </style>
  </noscript>
`}
/>

### Next.js Example

If you are using the Pages Router, you can add the `noscript` tag in the
`_document.js` file.

If you are using the App Router, you can add the `noscript` tag in the
`layout.js` file.

This will ensure that the `noscript` tag is rendered on the server side for
both the Pages and App Router.

<Image
  alt="Example of banner for no JavaScript on Digital Collections"
  border="1px solid var(--nypl-colors-ui-border-default)"
  src={progressiveEnhancementExample}
  {...exampleWrapperStyles}
/>

<Banner
  content={
    <>
      <strong>IMPORTANT:</strong> The above example needs to be modified to work
      appropriately in Next.js. See the{" "}
      <a href="https://nextjs.org/docs/app/getting-started/css">
        Next.js documentation
      </a>{" "}
      for other approaches.
    </>
  }
  variant="informative"
/>

<Source
  language="html"
  code={`
  <style>
    {
      ".noscript-banner {background: #f9e08e;color: #2e2e2e;padding: 16px;text-align: center;font-size: 16px;}"
    }
  </style>
`}
/>

## Resources

- [MDN Progressive Enhancement](https://developer.mozilla.org/en-US/docs/Glossary/Progressive_Enhancement)
- [It's About Time I Tried to Explain What Progressive Enhancement Actually Is](https://piccalil.li/blog/its-about-time-i-tried-to-explain-what-progressive-enhancement-actually-is/)
