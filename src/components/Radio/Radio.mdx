import {
  ArgTypes,
  Canvas,
  Description,
  Meta,
} from "@storybook/addon-docs/blocks";

import ComponentChangelogTable from "../../utils/ComponentChangelogTable";
import ComponentDocsHeader from "../../utils/components/ComponentDocsHeader";
import Link from "../Link/Link";
import * as RadioStories from "./Radio.stories";
import { changelogData } from "./radioChangelogData";

<Meta of={RadioStories} />

<ComponentDocsHeader
  category="Form elements"
  componentName="Radio"
  summary="Input element that allows a user to select a single item from a list of related options"
  versionAdded="0.22.0"
  versionLatest="Prerelease"
/>

<Canvas of={RadioStories.Controls} />

## Table of contents

- [Overview](#overview)
- [Component props](#component-props)
- [Accessibility](#accessibility)
- [Browser states](#browser-states)
- [Helper and error text](#helper-and-error-text)
- [Label using JSX elements](#label-using-jsx-elements)
- [Changelog](#changelog)

## Overview

`Radio` renders a radio button form element.

Note that all the examples below show the `Radio` button in isolation. We
recommend to always use the `Radio` component inside a Design System `RadioGroup`
component. The `RadioGroup` component will handle all the states and data
management.

## Component props

`Radio` composes an HTML `input` element, so you may pass any HTML `input`
attributes, as any Chakra style props and the props below.

<ArgTypes of={RadioStories.Controls} />

## Accessibility

`Radio` renders an `input` element with the `type="radio"` attribute. This HTML
element is accessible by default.

When `showLabel` is set to false, the `labelText` value will be set to the
`<input>`'s `aria-label` attribute.

Resources:

- [W3C ARIA radiogroup and radio example](https://www.w3.org/TR/2016/WD-wai-aria-practices-1.1-20160317/examples/radio/radio.html)
- [W3C Radio Button Example](https://www.w3.org/TR/2017/WD-wai-aria-practices-1.1-20170628/examples/radio/radio-1/radio-1.html)
- [MDN input type="radio"](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/radio)
- [Deque University Radio and Radio Group](https://dequeuniversity.com/library/aria/radio-and-radio-group)
- [Chakra UI Radio](https://chakra-ui.com/docs/components/form/radio)

## Browser states

<Canvas of={RadioStories.BrowserStates} />

## Helper and error text

<Canvas of={RadioStories.HelperAndErrorText} />

## JSX element labels

If you need to add dynamic content or a layout to the label, you can pass a JSX
element. Another example may be found in the [`RadioGroup` documentation](http://localhost:6006/?path=/docs/components-form-elements-radiogroup--docs#labels-using-jsx-elements).

<Canvas of={RadioStories.LabelUsingJSXElements} />

## Changelog

<ComponentChangelogTable changelogData={changelogData} />
