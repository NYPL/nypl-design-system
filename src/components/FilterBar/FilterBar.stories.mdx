import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
} from "@storybook/addon-docs/blocks";
import { withDesign } from "storybook-addon-designs";
import { HStack } from "@chakra-ui/react";

import { getCategory } from "../../utils/componentCategories";
import DSProvider from "../../theme/provider";
import FilterBar from "./FilterBar";
import MultiSelect from "./../MultiSelect/MultiSelect";
import FilterBarStory from "./FilterBarStory";

<Meta
  title={getCategory("FilterBar")}
  component={FilterBar}
  decorators={[withDesign]}
  parameters={{
    design: {
      type: "figma",
      url:
        "https://www.figma.com/file/qShodlfNCJHb8n03IFyApM/Main?node-id=11895%3A658",
    },
    //jest: ["Checkbox.test.tsx"],
  }}
  argTypes={{
    id: { table: { disable: true } },
  }}
/>

# FilterBar

| Component Version | DS Version |
| ----------------- | ---------- |
| Added             | `0.1.0`    |
| Latest            | `0.25.0`   |

<Description of={FilterBar} />

This component renders a FilterBar.

<Canvas withToolbar>
  <Story
    name="FilterBar"
    args={{
      id: "filter-bar-basic",
      label: "Filter By:",
      isModalOpen: false,
      isMobile: false,
      selectedItems: [],
    }}
  >
    {(args) => <FilterBar {...args} />}
  </Story>
</Canvas>

<ArgsTable story="FilterBar" />

## FilterBar

<Canvas>
  <Story
    name="FilterBar With MultiSelects"
    args={{
      id: "filter-bar-with-multiselects",
      label: "Filter By:",
      isModalOpen: false,
      isMobile: false,
      //selectedItems: {},
    }}
  >
    {(args) => (
      <FilterBarStory
        {...args}
        id="filter-bar-multiselect"
        label="Filter By:"
      />
    )}
  </Story>
</Canvas>

<ArgsTable story="FilterBar With MultiSelect" />
