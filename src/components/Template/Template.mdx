import { ArgTypes, Canvas, Description, Meta, Source } from "@storybook/blocks";
import ComponentChangelogTable from "../../utils/ComponentChangelogTable";
import { changelogData } from "./templateChangelogData";

import * as TemplateStories from "./Template.stories";
import {
  Template,
  TemplateBreakout,
  TemplateTop,
  TemplateSidebar,
  TemplateMain,
  TemplateMainNarrow,
  TemplateBottom,
} from "./Template";
import Banner from "../Banner/Banner";
import { Box } from "@chakra-ui/react";
import Heading from "../Heading/Heading";
import Image from "../Image/Image";
import Link from "../Link/Link";
import List from "../List/List";
import Table from "../Table/Table";
import Text from "../Text/Text";

import templateBreakout from "/static/template/templateBreakout.png";
import templateBottom from "/static/template/templateBottom.png";
import templateMain from "/static/template/templateMain.png";
import templateMainNarrow from "/static/template/templateMainNarrow.png";
import templateMainWide from "/static/template/templateMainWide.png";
import templateSidebarLeft from "/static/template/templateSidebarLeft.png";
import templateSidebarRight from "/static/template/templateSidebarRight.png";
import templateTop from "/static/template/templateTop.png";
import ComponentDocsHeader from "../../utils/components/ComponentDocsHeader";
import ComponentVersion from "../../utils/components/ComponentVersionTable";
import DocsWithImage from "../../utils/components/DocsWithImage";
import { exampleWrapperStyles } from "../../utils/utils.ts";

<Meta of={TemplateStories} />

<ComponentDocsHeader
  category="Page layout component"
  componentName="Template"
  summary="A structural skelton for composing a standard page layout"
  versionAdded="0.3.6"
  versionLatest="Prerelease"
/>

<Canvas of={TemplateStories.WithControls} />

## Table of Contents

- {<Link href="#overview" target="_self">Overview</Link>}
- {<Link href="#component-props" target="_self">Component Props</Link>}
- {<Link href="#accessibility" target="_self">Accessibility</Link>}
- {<Link href="#child-components" target="_self">Child Components</Link>}
- {<Link href="#sidebar-configuration" target="_self">Sidebar Configuration</Link>}
- {<Link href="#full-page-layout" target="_self">Full Page Layout</Link>}
- {<Link href="#changelog" target="_self">Changelog</Link>}

## Overview

- The `Template` component is used to render the general structure of a page.
- The `Template` child components should be used to compose the basic page
  layout.
- The child components adhere to NYPL design standards and provide a consistent
  mobile and desktop layout.

<Source
  code={`
import {
  Template,
  TemplateBreakout,
  TemplateTop,
  TemplateSidebar,
  TemplateMain,
  TemplateBottom
} from "@nypl/design-system-react-components";
//...
<Template sidebar="...">
  <TemplateBreakout>...</TemplateBreakout>
  <TemplateTop>...</TemplateTop>
  <TemplateSidebar>...</TemplateSidebar>
  <TemplateMain>...</TemplateMain>
  <TemplateBottom>...</TemplateBottom>
</Template>
`}
  language="jsx"
/>

## Component Props

<ArgTypes of={TemplateStories.WithControls} />

## Accessibility

Wide lines of text are difficult to read and are proven to make it harder for
people to focus, while short lines of text force the eye to travel back too
often, breaking a reader's rhythm. The `TemplateMain` component has a max width
of 1280px, which is too wide for text. If your content includes long chunks of
text, you should use the `TemplateMainNarrow` component instead. Note that
`TemplateMainNarrow` is not meant to be used in conjunction with a sidebar.

This means you may use `TemplateMain` or `TemplateMainNarrow` depending on your
needs, but the two should not be used in conjunction.

The `TemplateMain` and `TemplateMainNarrow` components render the content as a
`<main>` element with a default `id` of `"mainContent"`. This should be used as
the anchor element that the skip navigation link points to. If your application
is using the current Header, it already contains the skip navigation feature but
make sure it is pointing to the correct anchor element.

Resources

- [WebAim Skip Navigation Link](https://webaim.org/techniques/skipnav/)
- [A11ymatters Skip Navigation Link](https://www.a11ymatters.com/pattern/skip-link/)

## Child Components

In addition to the root `Template` component, the following child components are
offered:

- `TemplateBreakout`
- `TemplateTop`
- `TemplateSidebar`
- `TemplateMain` or `TemplateMainNarrow`
- `TemplateBottom`

<Banner
  content={
    <>
      <strong>IMPORTANT:</strong> The DOM order of the child components should
      match the structure of the rendered page in the browser.
      <Text noSpace>
        If the sidebar is to be rendered on the left side of the page,{" "}
        <code>TemplateSidebar</code> should come before{" "}
        <code>TemplateMain</code> in the DOM. If the sidebar is to be rendered
        on the right side of the page, <code>TemplateSidebar</code> should come
        after <code>TemplateMain</code> in the DOM.
      </Text>
    </>
  }
  mb="s"
  type="recommendation"
/>

### TemplateBreakout

{

<DocsWithImage
  docs={
    <List type="ul">
      <li>
        Positioned directly below the NYPL universal header and above any other
        <code>Template</code> child components
      </li>
      <li>
        Not contained by the central content area and spans the full width of
        the viewport
      </li>
      <li>
        All components that go in <code>TemplateTop</code> component should be
        wrapped in a <code>div</code> element or a <code>Box</code> component
      </li>
      <li>
        Ideal for <code>Breadcrumbs</code>, <code>FilterbarInline</code>,{" "}
        <code>Hero</code>, and <code>SubNav</code> components
      </li>
      <li>
        Also recommended for custom UI that spans the full width of the viewport
      </li>
    </List>
  }
  imageProps={{
    alt: "Template breakout region",
    caption: "Breakout region",
    src: "/static/template/templateBreakout.png",
  }}
/>
}

### TemplateTop

{

<DocsWithImage
  docs={
    <List type="ul">
      <li>
        Positioned directly below <code>TemplateBreakout</code> and above{" "}
        <code>TemplateSidebar</code> and <code>TemplateMain</code>
      </li>
      <li>Contained by the central content area</li>
      <li>Always spans the full width of the central content area</li>
      <li>
        Ideal for <code>Banner</code> and <code>FilterbarInline</code>{" "}
        components
      </li>
      <li>Also recommendaed for custom "announcement" elements</li>
    </List>
  }
  imageProps={{
    alt: "Template top region",
    caption: "Top region",
    src: "/static/template/templateTop.png",
  }}
/>
}

### TemplateSidebar

{

<DocsWithImage
  docs={
    <List type="ul">
      <li>
        Positioned below <code>TemplateTop</code> and to either the left or
        right of <code>TemplateMain</code>
      </li>
      <li>
        Not recommended for use with <code>TemplateMainNarrow</code>
      </li>
      <li>
        Spans a perecentage of the central content area based on the current
        breakpoint
      </li>
      <li>Ideal for filtering elements and secondary menus</li>
    </List>
  }
  imageProps={{
    alt: "Template sidebar region",
    caption: "Sidebar region",
    src: "/static/template/templateSidebarLeft.png",
  }}
/>
}

### TemplateMain

{

<DocsWithImage
  docs={
    <List type="ul">
      <li>
        Positioned below <code>TemplateTop</code> and to either the left or
        right of
        <code>TemplateSidebar</code>, when it is rendered
      </li>
      <li>
        May be used in conjunction with <code>TemplateSidebar</code>
      </li>
      <li>
        Spans a perecentage of the central content area when the sidebar is
        rendered
      </li>
      <li>
        Spans the full width of the central content area when the sidebar is not
        rendered
      </li>
      <li>
        Content in this region should align with the{" "}
        <Link href="/?path=/docs/style-guide-responsive-grid--docs">
          responsive grid
        </Link>{" "}
        recommendations
      </li>
    </List>
  }
  imageProps={{
    alt: "Template main region",
    caption: "Main region",
    src: "/static/template/templateMain.png",
  }}
/>
}

### TemplateMainNarrow

{

<DocsWithImage
  docs={
    <List type="ul">
      <li>
        Positioned below <code>TemplateTop</code> and to either the left or
        right of
        <code>TemplateSidebar</code>, when it is rendered
      </li>
      <li>
        Should not be used in conjunction with <code>TemplateSidebar</code>
      </li>
      <li>Contained by the central content area</li>
      <li>
        Max width of <code>720px</code>
      </li>
      <li>Ideal for displaying content that is primarily text</li>
    </List>
  }
  imageProps={{
    alt: "Template main narrow region",
    caption: "Main narrow region",
    src: "/static/template/templateMainNarrow.png",
  }}
/>
}

### TemplateBottom

{

<DocsWithImage
  docs={
    <List type="ul">
      <li>
        Positioned directly below <code>TemplateSidebar</code> and{" "}
        <code>TemplateMain</code> and above the NYPL universal footer
      </li>
      <li>Contained by the central content area</li>
      <li>Always spans the full width of the central content area</li>
      <li>
        Ideal for <code>Banner</code>, <code>FeaturedContent</code>,{" "}
        <code>NewsletterSignup</code> components
      </li>
      <li>Also recommended for custom "announcement" elements</li>
    </List>
  }
  imageProps={{
    alt: "Template bottom region",
    caption: "Bottom region",
    src: "/static/template/templateBottom.png",
  }}
/>
}

## Sidebar Configuration

Use the `sidebar` prop to set the sidebar

- `left`
- `right`
- `none` (default)

<Source
  code={`
<Template sidebar="left">
  ...
</Template>
`}
  language="jsx"
/>

{

<Box
  display={{ base: "block", md: "grid" }}
  gridTemplateColumns="1fr 1fr 1fr"
  gap="l"
>
  <Box>
    <Heading level="h3" size="heading7">
      Left
    </Heading>
    <Image
      alt="Template with left sidebar"
      border="1px solid var(--nypl-colors-ui-border-default)"
      src="/static/template/templateSidebarLeft.png"
      {...exampleWrapperStyles}
    />
  </Box>
  <Box>
    <Heading level="h3" size="heading7">
      Right
    </Heading>
    <Image
      alt="Template with right sidebar"
      border="1px solid var(--nypl-colors-ui-border-default)"
      src="/static/template/templateSidebarRight.png"
      {...exampleWrapperStyles}
    />
  </Box>
  <Box>
    <Heading level="h3" size="heading7">
      None
    </Heading>
    <Image
      alt="Template without sidebar"
      border="1px solid var(--nypl-colors-ui-border-default)"
      src="/static/template/templateSidebarNone.png"
      {...exampleWrapperStyles}
    />
  </Box>
</Box>

}

## Full Page Layout

{
<DocsWithImage
  docs={
    <>
      The <code>Tempate</code> component should be used with the NYPL universal <code>Header</code>
      and <code>Footer</code> to compose a pages aligned with NYPL design and application
      standards
      <Banner
        content={
          <>
            <strong>IMPORTANT:</strong> If an app needs to render the{" "}
            <code>SkipNavigation</code> component, it needs to be explicitly rendered
            immediately before the <code>Template</code> component.
          </>
        }
        mt="s"
        type="informative"
      />

    </>

}
imageProps={{
    alt: "Full page layout",
    src: "/static/template/templateFullPageLayout.png",
  }}
/>
}

<Source
  code={`
import {
  SkipNavigation,
  Template,
  TemplateBreakout,
  TemplateTop,
  TemplateSidebar,
  TemplateMain,
  TemplateBottom
} from "@nypl/design-system-react-components";
//...
<style>
  #header-placeholder {
    min-height: 70px;
  }
  @media screen and (min-width: 1024px) {
    #header-placeholder {
      min-height: 230px;
    }
  }
</style>
<!-- .... -->
<div id="header-placeholder">
  <div id="nypl-header"></div>
  <script
    type="module"
    src="https://ds-header.nypl.org/header.min.js?containerId=nypl-header"
    async
  ></script>
</div>
<SkipNavigation />
<Template sidebar="...">
  <TemplateBreakout>...</TemplateBreakout>
  <TemplateTop>...</TemplateTop>
  <TemplateSidebar>...</TemplateSidebar>
  <TemplateMain>...</TemplateMain>
  <TemplateBottom>...</TemplateBottom>
</Template>
<div id="nypl-footer"></div>
<script type="module" src="https://ds-header.nypl.org/footer.min.js?containerId=nypl-footer" async></script>
`} language="jsx" />

## Changelog

<ComponentChangelogTable changelogData={changelogData} />
