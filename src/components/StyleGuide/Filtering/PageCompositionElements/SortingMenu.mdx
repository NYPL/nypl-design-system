import { Meta } from "@storybook/addon-docs/blocks";

import ComponentDocsHeader from "../../../../utils/components/ComponentDocsHeader";
import Image from "../../../Image/Image";
import { exampleWrapperStyles } from "../../../../utils/utils.ts";

<Meta title="Style Guide/Filtering/Page elements/Sorting menu" />

<ComponentDocsHeader category="Style Guide" componentName="Sorting menu" />

The `sorting` menu updates how records are ordered in a results set.

<Image
  alt="Example of sorting menu"
  src="/filteringStyleGuide/sortingMenu.png"
  {...exampleWrapperStyles}
/>

## Table of contents

- [Rendering](#rendering)
- [Position](#position)
- [Labeling](#labeling)

## Rendering

The `sorting menu` should be rendered using the DS
[Menu](./?path=/docs/components-navigation-menu--docs) component.

## Position

The `sorting menu` should be positioned below the `active filters` list and aligned
to the right.

## Labeling

The initial text label and `aria-label` value for the sorting menu should be set
to "Sort by." If a sorting option is selected, the `aria-label` value should
remain as "Sort by," but the text label for the sorting menu should be updated
to include the label of the option that was selected.

Label example:

`Sort by: Title A-Z`
