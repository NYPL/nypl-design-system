import { IProcessOptions } from './interfaces/IProcessOptions';
/**
 * Synchronously extract and replace CSS selectors from a string.
 */
export declare const processSync: ({css, filters, postcssSyntax, preserveLines}: IProcessOptions) => string;
/**
 * Asynchronously extract and replace CSS selectors from a string.
 */
export declare const process: (options: IProcessOptions) => Promise<{}>;
declare const _default: {
    process: (options: IProcessOptions) => Promise<{}>;
    processSync: ({css, filters, postcssSyntax, preserveLines}: IProcessOptions) => string;
};
export default _default;
